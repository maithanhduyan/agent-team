{
  "name": "agent-desktop",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "agent-desktop memory foundation — core memory module (T03): append-only L2 writer (sessions.jsonl), L3 writer (core.md), provenance + anti-poisoning guardrails, SEC-MEM-01 render wrapper; retrieval tools (T04): search_memory (α·sim+β·recency+γ·importance), grep_logs (RE2-safe), hot-fact injection, SEC-MEM-02 trust guidance, agentic query budget; consolidation job (T05): sleep-time pipeline (reflect → graduation → multi-model judge gate → verifier → write), conflict supersede, Day-30 decay, per-model cost caps (spec §8–§10, Q5); Telegram bridge (T08): consolidation notifications + spend report (SEC-COST-02), /memory chat commands (search/grep/hot/spend), sandbox-first transport (no network), SEC-MEM-01/SEC-KEY/SEC-LOG compliant",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "test:watch": "node --import tsx --test --watch test/*.test.ts",
    "consolidate": "node --import tsx src/cli-consolidate.ts",
    "consolidate:built": "node dist/cli-consolidate.js",
    "bridge:sandbox": "node --import tsx src/cli-bridge.ts --sandbox",
    "bridge:sandbox:built": "node dist/cli-bridge.js --sandbox",
    "bridge": "node --import tsx src/cli-bridge.ts",
    "bridge:built": "node dist/cli-bridge.js",
    "typecheck:evolution": "tsc --noEmit -p evolution/tsconfig.json",
    "test:evolution": "node --import tsx --test evolution/test/*.test.ts",
    "eval:dataset": "node --import tsx evolution/src/build-eval-dataset.ts",
    "eval:scan": "node --import tsx evolution/src/secret-scan.ts"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
